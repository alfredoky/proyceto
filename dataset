 protected void Page_Load(object sender, EventArgs e)
    {

        if (Session["usernamee"] == null)
        {
            Response.Redirect("~/account/login.aspx");
        }

        ServiceReference1.ServiceSoapClient ws = new ServiceReference1.ServiceSoapClient();
        DataSet ds = ws.getData();

        try
        {
            Label1.Text = ds.Tables[0].Rows[0][0].ToString();
            Label2.Text = ds.Tables[0].Rows[0][1].ToString();
            Label3.Text = ds.Tables[0].Rows[0][2].ToString();
        }

        catch
        {
            ScriptManager.RegisterStartupScript(this, GetType(), "alertMessage", "alert('No tiene resultados aun, porfavor complete su encuesta!');", true);


        }
        
    }
